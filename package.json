{
  "name": "agentreadyvideo-site",
  "version": "0.1.0",
  "private": true,
  "description": "Public home of the Agent-Ready Video (ARV) standard.",
  "license": "Apache-2.0",
  "type": "module",
  "scripts": {
    "sync": "node scripts/sync-standard.mjs",
    "build": "npm run sync && node build.mjs",
    "preview": "npm run build && node scripts/serve.mjs"
  },
  "engines": {
    "node": ">=18"
  }
}
